# ElwindUI implementation status

Snapshot: 2026-08-30. Desired behavior is defined by [`../specs/README.md`](../specs/README.md).

Legend: ✅ implemented/verified, 🚧 partial, ⬜ not implemented.

## Core and language

| Area | State | Current evidence / gap |
|---|---|---|
| Component frontend and `view!` DSL | ✅ | proc-macro frontend, parser, metadata-driven scalar/collection `#[content]` validation and lowering, named `elwindui::new!` construction planning, and code generation are in workspace tests |
| ControlTemplate | 🚧 | explicit `template_view!(|alias: Target| { ... })` standalone/reusable values, component `template: template_view!(|alias: Self| { ... })` defaults, exact-type `EnvironmentContext::set_control_template::<C>(Some/None)` mount-time selection, typed/reactive parent aliases when the target exports the corresponding capability, and one shared semantic planner/emitter (ordinary `view!` plus component/standalone templates) for construction, property/content, event/two-way, lifecycle, dynamic regions, `EnvironmentScope`, deferred views, let/reference, semantic Brush, ownership, cleanup, and `ContentPresenter`/ContentControl logical-Visual separation are implemented and tested ([#187](https://github.com/puchinya/elwindui/issues/187), [#199](https://github.com/puchinya/elwindui/issues/199)); standalone targets are never inferred from expected types, `Self` is component-default-only, reusable templates are ordinary Rust functions, and the public `#[control_template]` marker API is removed; property-free templates accept any valid `ControlExt` target, while typed property paths require generated `TemplateProperty<KEY>`/`WritableTemplateProperty<KEY>` bridges, with analysis-only `cfg(rust_analyzer)` shadows preserving the exact associated value type and writable surface; raw framework/class-managed property bridges are not guaranteed; runtime re-template/per-instance template/TemplatePart/VisualState remain out of scope |
| ControlTemplate | 🚧 | explicit `template_view!(|alias: Target| { ... })` standalone/reusable values, component `template: template_view!(|alias: Self| { ... })` defaults, exact-type `EnvironmentContext::set_control_template::<C>(Some/None)` mount-time selection, typed/reactive parent aliases when the target exports the corresponding capability, and one shared semantic planner/emitter (ordinary `view!` plus component/standalone templates) for construction, property/content, event/two-way, lifecycle, dynamic regions, `EnvironmentScope`, deferred views, let/reference, semantic Brush, ownership, cleanup, and `ContentPresenter`/ContentControl logical-Visual separation are implemented and tested ([#187](https://github.com/puchinya/elwindui/issues/187), [#199](https://github.com/puchinya/elwindui/issues/199)); standalone targets are never inferred from expected types, `Self` is component-default-only, reusable templates are ordinary Rust functions, and the public `#[control_template]` marker API is removed; property-free templates accept any valid `ControlExt` target, while typed property paths require generated `TemplateProperty<KEY>`/`WritableTemplateProperty<KEY>` bridges, with analysis-only `cfg(rust_analyzer)` shadows preserving the exact associated value type and writable/read-only capability; raw framework/class-managed property bridges are not guaranteed; runtime re-template/per-instance template/TemplatePart/VisualState remain out of scope. Final remediation evidence: direct Environment installation, named inherited-content forwarding before child mount, and layout/RenderTree are covered by `crates/elwindui/tests/control_template.rs` (9 tests); the AppKit demo screenshot shows the Environment override, reactive alias label, and ContentPresenter content while the default-only marker is absent. WinUI3 and GTK4 remain unverified. |
| `param` / `prop` / `state` / `computed` / `bindable` | ✅ | generated storage, notifications, and dependency refresh are used by examples |
| Once / OneWay / TwoWay binding | ✅ | component and ViewModel examples exercise generated paths |
| `if` / `match` dynamic regions | ✅ | conditional region replacement is implemented |
| `for` dynamic regions | ✅ | supported `Rc` items retain stable identity; other supported collections rebuild children, and nested dynamic regions are generated recursively |
| Static diagnostics | 🚧 | main DSL validations exist; cross-file resolution and some planned rules are absent |
| Class hierarchy macro | ✅ | ordinary/root/trait-only/struct-only forms are used throughout core/backends |
| Component override dispatch bridge | ✅ | view-component companion `#[overridable]`/`#[overrides]` metadata is re-emitted into the generated `#[class]` impl, so normal `Rc<dyn UIElementExt>` host paths use the existing ancestor override chain; runtime probe covers hit-test, measure, arrange, and RenderTree render dispatch ([#185](https://github.com/puchinya/elwindui/issues/185)) |
| UI tree, layout, routed events, focus | ✅ | core tests and AppKit runtime examples cover the implemented model |
| Window lifecycle and presentation | ✅ | mount/show/hide/close behavior remains implemented; `transparent` and `always_on_top` default false and map to AppKit `NSWindow` plus WinUI 3 root/presenter state ([#80](https://github.com/puchinya/elwindui/issues/80), [#125](https://github.com/puchinya/elwindui/issues/125), [#150](https://github.com/puchinya/elwindui/issues/150)); `show`/`hide`/`close` are now generated as ordinary `#[overrides]` methods reached through the normal `#[class]` ancestor chain, replacing the earlier CI-8 inherent-method/UFCS workaround — externally observable Window lifecycle behavior is unchanged ([#128](https://github.com/puchinya/elwindui/issues/128), done, verified on macOS AppKit; WinUI 3 unverified in this environment); `Window` also gains `#[overridable]` `mount_override`/`unmount_override` framework hooks (not a second user-authored lifecycle surface — `mount()`/`unmount()` stay fixed, non-overridable algorithms) plus a `#[doc(hidden)] WindowLifecycleHost::set_close_request_handler` capability the generated host-composition `Window`'s own `#[overrides] mount_override`/`unmount_override` installs/clears; `unmount_override` closes the window's own active custom popup/context-menu surface before its content unmounts, and a native close affordance (AppKit `windowShouldClose:`, WinUI 3 `AppWindow.Closing`) now routes through the same close-request handler a programmatic `WindowExt::close()` reaches, so a user clicking the native close box enters the identical lifecycle ([#162](https://github.com/puchinya/elwindui/issues/162); AppKit `windowShouldClose:` compile-verified, WinUI 3 `AppWindow.Closing` unverified in this environment, interactive click-through verification on either backend outstanding) |
| `#[class]` `struct_only` override transparency | ✅ | a `struct_only` implementor of any class-managed interface (`trait_only`, ordinary, or root class) is a transparent bridge for that interface's `#[overridable]`/`#[overrides]` contract at arbitrary ordinary-descendant depth and across real crate boundaries, with no manually-authored `{ConcreteName}Ext` compatibility alias required even when the `struct_only` concrete type's own bare name differs from the interface it implements (`docs/design/tools/class_macro_design.md`, `docs/specs/macro_class_spec.md` §5); regression-tested same-crate (2-hop, 4-hop, independent multi-slot resolution, same-crate different-name `struct_only`, a generic `struct_only<T>` preserving its own `impl_generics`/`where_clause`, `struct_only` targeting an ordinary (non-root) interface's own generated bridge, explicit `no_ancestor_forward` bypass — `crates/elwindui-core/src/ui/testsupport.rs`), via a dedicated 3-crate fixture with no backend-side alias (`elwindui-bridge-fixture-iface`/`-backend` + `crates/elwindui/tests/class_bridge_cross_crate.rs`), via focused macro-expansion-level tests (`crates/elwindui-macros/src/class.rs`'s own `class_interface_bridge_tests` module), and in real production cross-crate usage (`Window`, `elwindui-core` → each backend → every example/consumer crate) ([#128](https://github.com/puchinya/elwindui/issues/128), PR #164, done — includes the PR #164 review remediation covering findings A1-A4). PR #164 final remediation round additionally covers: a `struct_only` implementor of a *root*-mode interface (e.g. `UIElement`-shaped classes), by requiring the implementor to also `inherits = <the same root class>` and forward `as_ui_element` to the composed storage — regression-tested same-crate (`BridgeRootBase`/`BridgeRootConcrete`/`BridgeRootDerived`, `testsupport.rs`) and cross-crate (`BridgeFixtureRoot`/`BridgeFixtureRootConcrete`/`BridgeFixtureRootDerived`, `class_bridge_cross_crate.rs`), plus a dedicated diagnostic test for a non-matching `inherits` (finding C2); and registry-independent resolution of whether a descendant's own `inherits = Ancestor<Args>` generic arguments reattach onto the ancestor's `{Name}Ext` trait, replacing an earlier same-crate-registry-based heuristic that could not work across real crate boundaries — regression-tested cross-crate for both a generic `struct_only` implementor of a non-generic interface (arguments never leak onto the real interface) and a generic ordinary ancestor (its own argument is kept) at both the immediate `inherits = ..` edge (finding A5) and every further recursive hop below it (finding A6, `BridgeFixtureGenericDeepDerived`/`GenericOrdinaryDeepDerived` in `class_bridge_cross_crate.rs`, plus a macro-expansion-level test proving the generated recursive metadata calls the ancestor's own policy macro rather than baking a resolved trait) — the former `ancestor_own_trait`/`struct_only_ancestor_in_this_crate` same-crate-registry functions are deleted, not merely superseded. Full workspace build/test/fmt verified green after this round. |
| Component lifecycle hooks | ✅ | `new()`/`mount(environment)`/build are separate generated phases (`docs/design/runtime/component_lifecycle_design.md`); `on_mount` fires after the component's own view builds and its children have mounted; `on_update(field, ...)` dispatches through the existing property-changed subscription machinery, excluding the initial construction-time value-set; `on_unmount` and recursive child-first subtree unmount are wired uniformly across all Component shapes, Window `close()`, and dynamic region removals (`unmount_subtree`, `__unmounted: Cell<bool>`, `add_unmount_hook`, subscription/Environment listener detachment) ([#80](https://github.com/puchinya/elwindui/issues/80), [#126](https://github.com/puchinya/elwindui/issues/126), done) |
| ReactiveGraph fallback API | ✅ | the unreachable `SignalId`/`ReactiveGraph` stub (no constructor, no callers anywhere in the workspace) was removed from `crates/elwindui-core/src/reactive.rs`; dependency tracking and change notification for `#[computed]`/`#[async_computed]`/`#[bindable]` fields continue to be handled entirely by `elwindui-codegen`'s static analysis plus `Subscription`/`ObservableExt` in the same file, which are unaffected ([#81](https://github.com/puchinya/elwindui/issues/81), done) |
| Global store / async computed | 🚧 | `#[elwindui::store] mod Name { .. }` (`Item::Store`/`StoreDef`, same field vocabulary as `viewmodel`), the process-wide `EnvironmentContext`-backed singleton (`Name::instance()`), and `#[async_computed(expr = ..)]` on both `viewmodel` and `store` (generation-counter "supersede, not cancel", `AsyncComputed<T>` `Loading`/`Ready`/`Failed` getter) are implemented and tested end-to-end, including a real cross-thread supersede (`crates/elwindui/tests/store_and_async_computed.rs`); `#[elwindui::main]` auto-installs a background `tokio` runtime (`elwindui_core::task::install_background_runtime`/`spawn_background`, verified against a genuinely suspending future by `crates/elwindui-core/tests/spawn_local_cross_thread_wake.rs`); validation rule 20 (`#[async_computed]` viewmodel/store-only) is implemented. **Not yet implemented**: the `view!`-side bare `TypeName.field` store-reference syntax and its auto-subscription codegen (dsl_spec.md §3), and validation rules 12 (store field-reference resolution, which checks exactly that unimplemented syntax) and 13 (`#[param]` isolation from store/viewmodel fields) — a store's fields can be read today from ordinary Rust code (`Name::instance().field()`, e.g. inside an action or `on_click`), just not yet bare-referenced directly inside a `view! { .. }` attribute expression. `#[undoable]`/undo-redo has been removed from the contract entirely (not deferred — ElwindUI does not support declarative undo/redo, matching SwiftUI's `UndoManager`-bridge approach); dsl_spec.md §13 rule 21 is retired (`(欠番)`) ([#82](https://github.com/puchinya/elwindui/issues/82)) |
| Text style cascade | ✅ | AppKit and WinUI 3 adapters exist; GTK4 is absent |
| Context menu and PopupSurface | 🚧 | platform-neutral `ContextRequest`, native NSMenu/MenuFlyout context menu, custom `ContextMenuPresenter`, and generic `PopupSurface` with AutoFlip placement and light dismiss are implemented; `context_popup` content is a `ViewFactory` (generic deferred View factory, `docs/design/runtime/view_factory_design.md`) evaluated at popup-open time against a popup-scoped derived `EnvironmentContext` with owner captured only `Weak` and a declarative `PopupDismissAction` (resolvable via `#[environment(popup_dismiss)]`, a framework built-in Environment key); popup close on both backends runs `unmount_subtree` (child-first `on_unmount`/subscription teardown) before the ElwindUI popup host detaches/clears its own content tree, on every dismissal path; framework-initiated close (e.g. `PopupDismissAction`) additionally unmounts before native visibility/detach on both backends, but WinUI3's native light-dismiss (`Popup.Closed`, which the toolkit fires only after it has already set `IsOpen=false`) is a documented exception to that stronger ordering — the portable, host-tree-detach guarantee still holds on that path ([#161](https://github.com/puchinya/elwindui/issues/161)); declarative `context_popup: view! { .. }` DSL sugar (reusing `view!`'s own grammar, deferred to popup-open time) is now implemented — a `context_popup` attribute assigned a bare `view! { .. }` block (as opposed to an ordinary `ViewFactory`-typed expression) is lowered at macro-expansion time into a hidden, framework-synthesized `ContentControl`-based Component/View pair (`elwindui-codegen::lower_deferred_views_in_module`) carrying a single synthetic `Weak<Owner>` field; the emitted `ViewFactory::new(..)` factory recovers the enclosing Component's own weak self at the top nesting level (`__self_weak`, the same mechanism `__build_view`'s `__most_derived` local already uses) and, for a `context_popup` nested inside another `context_popup`'s own content, reuses the outer hidden Component's own already-correct `__view_owner` field directly instead of re-deriving it through a downcast that only works at the top level — and constructs a fresh hidden-Component instance on every popup open, so bare names inside the block resolve directly against the *enclosing* Component's own fields/`view!` scope. No second popup-specific binding/resolution engine was introduced: the existing `ViewClosureRewriter` (raw `on_mount`/`on_unmount`/`on_update`/event-handler Rust) was generalized once for an implicit-owner fallback gated by a source-Component field-readable/writable schema (PR #165 final rereview remediation, A2 — not "any unshadowed bare name falls back to the owner"), plus a genuine lexical-scope stack; the existing weak-owner (`ControlTemplate`'s own `templated_parent` pattern, generalized) and Environment-propagation (`node.environment_scope`, also generalized) mechanisms are reused unchanged. A direct, source-qualified 2-segment path (`vm.label`, `vm.save`, no intermediate nested Component) and a direct bare source field used inside the deferred body both build correctly and live-update while the popup stays open (PR #165 post-final rereview remediation, A8/A9 — `ImplicitOwnerDef::bindable_fields`/`reactive_fields`, a dedicated `ObservableExt` subscription bridged through `__view_owner` for a `#[bindable]` owner). Returns `None` (no popup shown) if the lexical owner has already been dropped by open time. This resolved the design blocker #162 originally identified (bare-name resolution against the enclosing Component's scope) without introducing a second resolution engine; AppKit runtime verified end-to-end (`crates/elwindui/tests/context_menu_and_popup.rs`), WinUI 3 backend implementation in progress with runtime verification separated into [#157](https://github.com/puchinya/elwindui/issues/157) ([#152](https://github.com/puchinya/elwindui/issues/152), [#161](https://github.com/puchinya/elwindui/issues/161), [#162](https://github.com/puchinya/elwindui/issues/162) implemented in PR #165, review in progress — not yet merged) |
| Theme / Environment | 🚧 | `EnvironmentKey`/`EnvironmentContext`/`#[environment(name)]` are implemented and tested (`crates/elwindui-core/src/environment.rs`, `crates/elwindui/tests/environment_field.rs`); `#[environment(name)]` fields resolve at mount-time (from the `EnvironmentContext` a component's generated `mount()` was called with), not construction-time — the earlier ambient thread-local `EnvironmentContext::current()`/`.enter()` propagation mechanism has been removed from the codebase entirely and superseded by explicit mount-time resolution (`docs/design/runtime/theme_environment_design.md`, [#80](https://github.com/puchinya/elwindui/issues/80), done); Theme is a thin Preset-over-Environment (`crates/elwindui-core/src/theme.rs`'s `Theme` trait, `#[elwindui::theme]`) — application-level, applied via `application_environment()` ([#96](https://github.com/puchinya/elwindui/issues/96), done); the `EnvironmentScope` DSL construct is now implemented (`EnvironmentScope { key: value, ..; <children> }` derives an overridden `EnvironmentContext` and mounts its children against it, producing no `UIElement`/Visual/Render/Layout node of its own; `if`/`match` directly inside a scope are scope-aware, `for` is not yet) ([#100](https://github.com/puchinya/elwindui/issues/100), done) — a DSL author can now express per-Window/subtree Environment (and therefore Theme-value) override by wrapping content in `EnvironmentScope` inside `Window { .. }`; Semantic Style is implemented: `BrushStyle`/`ResolvedValue`, 11組み込みsemantic Brush Key、alias/cycle-safe resolver、および`foreground`/`background`/`fill`/`stroke`のeffective-Environment解決・live再同期・`PlatformDefault` clearを提供し、Theme/EnvironmentScopeを含むend-to-end testは`crates/elwindui/tests/semantic_brush_style.rs`にある ([#97](https://github.com/puchinya/elwindui/issues/97), done); automatic native-control default-appearance stylingは引き続き提供せず、Native Style ([#98](https://github.com/puchinya/elwindui/issues/98)) が残る; `#[environment(name)]`/`EnvironmentScope` can now also resolve an Environment Key declared in a *different* crate via a completely-qualified crate path (`#[environment(some_crate::locale)]`, `EnvironmentScope { some_crate::locale: value }`) — `#[elwindui::environment_key]` additionally exports a `__elwindui_environment_key_{name}!` cross-crate macro (`docs/design/tools/environment_key_macro_design.md`), tested end to end by `crates/elwindui-environment-key-fixture` + `crates/elwindui/tests/environment_field_cross_crate.rs`/`environment_scope_cross_crate.rs` ([#129](https://github.com/puchinya/elwindui/issues/129), done) |
| File dialog | ✅ | current platform service implementation exists |
| Clipboard, notifications, drag and drop | ⬜ | no complete cross-platform implementation |

## Graphics

| Area | State | Current evidence / gap |
|---|---|---|
| Color, brushes, gradients, stroke, path | ✅ | core values and AppKit/WinUI rendering are present |
| Raster image | ✅ | loading/value model and both primary backend paths are present |
| Vector image / SVG | 🚧 | parser, limits, rendering, and many effects are present; both primary backends have documented filter/blend/mask fallbacks |
| Retained rendering and reconciliation | ✅ | active tree groups and native child reconciliation are implemented |
| Snapshot testing | 🚧 | tree dump exists; canvas/image snapshot assertion remains unimplemented |

## Samples

`control-template-demo`, `controls-demo`, `font-demo`, `graphics-demo`, `inheritance-demo`, `mascot-demo`, `notepad`, `theme-demo`, and `viewmodel-attr-demo` exercise the implemented public surface. A sample demonstrates usage but is not itself normative evidence.

## External generated-component DSL (#191/#193)

Qualified external generated-component paths in `view!` were delivered by merged PR #192. The
shared resolver keeps the authored construction/type path and maps the defining crate's
`#[macro_export]` props shape to its crate root across ordinary/template/dynamic lowering,
property/content/event/two-way wiring, and resync. Public external shape membership and setter
types come from `ComponentPublicShape::writable_fields`; source fields only contribute compatible
attributes. Issue #193's `elwindui::new!` named construction is implemented by PR #195: required
`#[param]`/`#[bindable]`, defaulted Params, ordinary mutable Props/content, full `Option` shape,
pre-mount initial setters, defining-crate-root constructor ABI macros, fixed-field resync rules,
and duplicate/unknown/non-writable/missing diagnostics are covered by focused codegen and
downstream tests. Initialization follows required construction, unmounted setup, defaulted
Params, initial Props/content, mount, and only then runtime binding/resync.

Directly-declared generated `Vec<Rc<T>>` content hosts publish one children change after raw
dynamic reconciliation, after the slot state is final and all temporary borrows are released.
Computed/template dependents therefore resync from the final collection. Inherited generated
`Vec<Rc<T>>` content without a redeclaration does not receive that host forwarding in #192; the
capability boundary is tracked in follow-up Issue #194, with no fake non-reactive bridge or
class-wide reactivity redesign.

The real two-crate downstream fixture
`crates/elwindui-external-component-fixture` plus
`crates/elwindui/tests/external_component_library.rs` covers external properties, collection
content, inherited scalar content, reactive resync, two-way wiring, template dynamic `if`/`for`,
named local/external construction, defaulted and required constructor inputs, Option Props, nested
module paths, and a Cargo alias. Unqualified imported external shorthand is not promised, and no
defining-crate `pub mod ui` facade is required. PR #192 is merged into the base; inherited generated
`Vec<Rc<T>>` content forwarding remains the separate follow-up Issue #194 and is intentionally not
changed by PR #195.

Current limitation for the approved unique-name scope: generated-component compile-time shape
identity is basename-based within a crate. Multiple generated components with the same basename in
different modules are not promised when they participate in shape resolution. This is an ElwindUI
compile-time ABI/registry limitation, not a fundamental Rust limitation; the path-aware registry and
#192/#193 shape-ABI redesign is tracked in [follow-up Issue #196](https://github.com/puchinya/elwindui/issues/196).
This does not make the implemented #193 unique-name local/external, nested-path, Cargo-alias, or
effective-inheritance scope incomplete.

## Primary gaps

- GTK4 is a stub and mobile backends have no implementation.
- ControlTemplateのruntime re-template/per-instance template/TemplatePart/VisualStateは未実装である ([#83](https://github.com/puchinya/elwindui/issues/83)); accessibility scaffolds and the NavigationHost/VirtualList/ErrorBoundary contract require a decision ([#85](https://github.com/puchinya/elwindui/issues/85)).
- Clipboard, drag/drop, and notifications are not complete.
- Language-server cross-file resolution, preview tooling, and hot reload are incomplete.
- Whole-workspace rust-analyzer diagnostics are not currently a clean gate. [#146](https://github.com/puchinya/elwindui/issues/146)/PR #169 eliminated the same-crate registry-ordering ghost diagnostics for `#[elwindui::component]`/`#[elwindui::theme]` via the `#[class]`-style `cfg(rust_analyzer)` dual expansion documented in `docs/design/tools/codegen_design.md` §3.2a, and PR #169's own review round additionally fixed a real (non-ghost) rust-analyzer-only regression in the shared Component shape classification (an unannotated `#[elwindui::component]` field with no initializer — the common case, `FieldKind::Prop` — was silently dropped from the rust-analyzer shadow's own constructor). Measured via `rust-analyzer diagnostics .`, compared against a same-commit baseline run: workspace-wide `Error`-severity diagnostics dropped from 24 to 17, with zero new diagnostics introduced; `examples/graphics-demo` is now fully clean (was 3 `E0107` errors from the same constructor-shape gap the shape fix corrects); `examples/mascot-demo` and `crates/elwindui/tests/window_mount_hide_close.rs` each lost one diagnostic from the same fix. Diagnostics confirmed **still present**, unrelated to #146/PR #169's own scope: `examples/theme-demo/src/main.rs`'s `view! { .. }` body has one rust-analyzer-only token-respacing artifact (present identically pre- and post-fix, reproducible with no dependency on same-crate registry ordering — a different root cause than the ordering ghosts this work targets); `examples/control-template-demo/src/main.rs` has two diagnostics (`no method __build on ControlTemplate<..>`, `no method panel on Rc<..>`) stemming from the generic rust-analyzer Component shadow deliberately not deriving ancestor `Ext` traits (e.g. `ControlExt`) or exposing `#[id(..)]`-named child accessors — both present identically pre- and post-fix; fixing either is a materially larger undertaking than a "generic Component shadow" fix and is explicitly out of scope (`docs/design/tools/codegen_design.md` §3.2a's own non-goals). Other pre-existing, unrelated diagnostics remain in `notepad` and `inheritance-demo`. PR #169's second review round (Issue #146 round 2: structural item-local/registry-dependent validation classification via `validate::ValidationErrors`, `component_public_shape`'s `ComponentConstructorReturn`/`on_*` exclusion, and `ComponentGenerationFailure::Classified`'s per-diagnostic routing — `docs/design/tools/codegen_design.md` §3.2a) was re-verified the same way against a same-commit baseline: the workspace-wide `Error`+`WeakWarning` diagnostic set from `rust-analyzer diagnostics .` is byte-for-byte identical before and after (738 lines both sides), i.e. zero diagnostics regressed and none were newly eliminated at this level — round 2 changed *how* the classification is computed and consumed (structurally rather than behaviorally, and without collapsing mixed diagnostics into one verdict), not which diagnostics fire, so this result is expected rather than a gap. The one exception is a single new benign `inactive-code` `WeakWarning` on `crates/elwindui-codegen/src/lib.rs` itself, from a legitimately-added `#[cfg(test)] mod` test block (same pattern as the dozens of pre-existing `#[cfg(test)]`-module warnings already present elsewhere in the workspace). PR #169's third review round (Issue #146 round 3: branch-sensitive `#[content(name)]` classification via `codegen::content_field_knowledge`, and making `component_public_shape` strictly source-local — `generate_component`/`generate_view` now thread a literal, un-flattened `source_component` alongside the existing ancestor-composed one, with own-field API membership (constructor/defaulted/readable/writable) driven from the shape and inherited-field forwarding kept on the pre-existing direct-computation fallback path — `docs/design/tools/codegen_design.md` §3.2a) was re-verified the same way against the round-2 (`73562ab`) baseline: the workspace-wide `Error`+`WeakWarning` diagnostic set from `rust-analyzer diagnostics .` is byte-for-byte identical, with zero diagnostics added, removed, or changed. This is expected — round 3, like round 2, changed how classification/shape input is computed and consumed, not which diagnostics fire. PR #169's fourth review round (Issue #146 round 4: `codegen::generate_component`'s view-less field loop now consumes `ComponentPublicShape`'s `constructor_params`/`defaulted_params`/`readable_fields`/`writable_fields` as the sole own-field API-membership authority, with `FieldDef` used only for storage/accessor-body implementation — closing the one remaining gap A2/AD-R3-6 left open — `docs/design/tools/codegen_design.md` §3.2a) was re-verified against the round-3 (`e548821`) baseline: the `Error`+`WeakWarning` diagnostic set is unchanged. This is a pure architectural de-duplication with no generated-API change — confirmed by all 235 pre-existing tests passing unmodified. PR #169's fifth review round (Issue #146 round 5: `generate_component`'s own setter parameter type — previously still taken from `strip_option`'s inner type or the field's own declared type, with the shape's own type tuple member discarded via `_` — now parsed from `ComponentPublicShape::writable_fields`'s own type entry, making the shape the complete setter-signature authority: existence, parameter type, and visibility) was re-verified against the round-4 (`2d28f7f`) baseline: the `Error`+`WeakWarning` diagnostic set is unchanged, and all 241 pre-existing tests passed unmodified, confirming the generated API is unchanged.
